function canPay(changeArray, totaldue) {
    console.log(totaldue);
    if(changeArray.length === 0);{
        return "Array is empty";
    }{
       let sum= (1 + 2+ 5);
       const result = sum > totaldue;
       return true;
    }{
        const result2 = sum < totaldue;
       return false;
    }
    

       
}

canPay =(cost,10);
const cost = (1,2,5);